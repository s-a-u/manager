<%= form_for(progect) do |f| %>
  <% if progect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(progect.errors.count, "error") %> prohibited this progect from being saved:</h2>

      <ul>
      <% progect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
